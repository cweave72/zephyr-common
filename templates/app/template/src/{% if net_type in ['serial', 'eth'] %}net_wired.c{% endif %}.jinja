/*******************************************************************************
 *  @file: net_wired.c
 *
 *  @brief: app_net_init() for the transports that need nothing beyond the
 *          shared connection-manager bring-up: serial (SLIP over uart_pipe) and
 *          wired ethernet. They differ only in which conf/ fragment applies.
*******************************************************************************/
#include "app.h"
#include "net_l4.h"

int
app_net_init(void)
{
    return app_net_l4_bringup();
}
